type Post {
  id: ID!
  title: String!
  description: String!
}

# Query Part
type Query {
  total_posts: Int!
  all_posts: [Post!]!
}

# Mutation Part
type Mutation {
  newPost(title: String! description: String!): Post!
}
