type Post {
  id: ID!
  title: String!
  description: String!
}

# Query Part
type Query {
  total_posts: Int!
  all_posts: [Post!]!
}

# Input type
input PostInput {
  title: String!
  description: String!
}

# Mutation Part
type Mutation {
  newPost(input: PostInput): Post!
}
